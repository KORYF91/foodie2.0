<li>
	{{name}}

	<button class="devoured" data-id="{{id}}" data-newsleep="{{devoured}}">
		{{#if devoured}}EaTen!!{{else}}NoT eAtEn!{{/if}}
	</button>

	<button class="delete-food" data-id="{{this.id}}">DELETE!</button>
</li>
